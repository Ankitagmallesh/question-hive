{
  "name": "@repo/utils",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/types": "*",
    "clsx": "^2.0.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
